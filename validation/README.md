# Validation directory

`run_public_validation.py` exercises the small validations that can be
reproduced without external data or precomputed libraries.

`historical/` contains four compact, author-generated reports plus one
machine-readable summary retained from the research work. They preserve the
conditions and caveats for the headline validation results; they are evidence,
not outputs regenerated by this public test suite.
