# Infrastructure

This graph-scale-independent support layer currently contains `log_config.py`,
the **CURRENT_REUSABLE** logging configuration consumed by training and
validation modules. It requires no generated research artifact and is kept
narrowly scoped to cross-cutting dependencies.
