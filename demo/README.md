# Demonstrations

Demonstrations are separate orchestration and presentation surfaces that use the
reusable research source under `src/project_risk/`.

- `exact_demo/` documents the compact runnable example in
  `examples/run_exact_example.py`.
- `visualization/` contains optional presentation code for compatible full-board
  states.
